export class Colour {
  colourCode: string
  hex: string
  name: string

  constructor (colourCode: string, hex: string, name: string) {
    this.colourCode = colourCode
    this.hex = hex
    this.name = name
  }
}
